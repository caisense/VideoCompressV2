可以。既然你已经有 **YOLOv8-seg 的 RK3588 板端实现**，我建议工程上采用：

> **YOLOv8-seg → segmentation importance map → 16×16 CU级 ROI/QP 分配 → RK3588 MPP H.265 → 限速发送 → PC标准H.265解码**

核心不是把 segmentation mask 发给 PC，而是**在 RK3588 编码前把 mask 转化为“哪里应该低 QP、哪里应该高 QP”**。PC 最终收到的仍然是标准 H.265 码流，不需要任何特殊解码器。

而且当前 Rockchip MPP 的 RK3588 ROI 实现正好适合这么做：`RoiRegionCfg` 支持绝对/相对 QP，RK3588 路径内部最终会生成 H.265 的 CU/CTU QP 配置，并通过 `KEY_ROI_DATA2` 随帧送给编码器。([GitHub](https://raw.githubusercontent.com/rockchip-linux/mpp/develop/utils/mpp_enc_roi_utils.h))

------

# 一、整体工程架构

我建议最终板端做成下面这样：

```text
                        RK3588
┌────────────────────────────────────────────────────────┐
│                                                        │
│ Camera / V4L2 / RKISP                                  │
│          │                                             │
│          ▼                                             │
│      Camera Frame                                      │
│       frame_id                                         │
│       pts_us                                           │
│          │                                             │
│          ├───────────────┐                             │
│          │               │                             │
│          ▼               ▼                             │
│        RGA          YOLOv8-seg / RKNN                   │
│    resize NV12           │                             │
│          │               ▼                             │
│          │         instance masks                      │
│          │         class/confidence                    │
│          │               │                             │
│          │               ▼                             │
│          │       Segmentation ROI Mapper               │
│          │               │                             │
│          │        16×16 Importance Map                 │
│          │               │                             │
│          │        ┌──────┴──────┐                      │
│          │        │             │                      │
│          │      CORE          EDGE                     │
│          │      HALO       BACKGROUND                  │
│          │        │             │                      │
│          │        └──────┬──────┘                      │
│          │               ▼                             │
│          │       QP / ROI rectangles                   │
│          │               │                             │
│          ▼               ▼                             │
│     MppFrame + ROI Metadata                            │
│              │                                         │
│              ▼                                         │
│         MPP H.265 Encoder                              │
│           40~48 kbps                                   │
│              │                                         │
│              ▼                                         │
│      Packetizer + Token Bucket                         │
│           ≤ 50 kbps                                    │
└──────────────┬─────────────────────────────────────────┘
               │
          Ethernet
          60 kbps
               │
               ▼
┌──────────────────────────────┐
│ PC                           │
│ UDP/RTP Receiver             │
│       ↓                      │
│ H.265 Decoder                │
│       ↓                      │
│ Display                      │
└──────────────────────────────┘
```

其中最重要的新模块就是：

```text
Segmentation ROI Mapper
```

这个模块建议你独立出来，不要把逻辑写死在 YOLO 后处理或者 MPP encoder 里面。

------

# 二、先明确一个关键点：不要只把 mask 转 bbox

如果你最后这么做：

```text
YOLO mask
    ↓
boundingRect()
    ↓
一个矩形 ROI
```

那其实 segmentation 的价值损失了一大半。

例如一个人：

```text
+--------------------+
|                    |
|       O            |
|      /|\           |
|      / \           |
|                    |
+--------------------+
```

bbox 是：

```text
      ┌────────┐
      │   O    │
      │  /|\   │
      │  / \   │
      └────────┘
```

大量 bbox 内的背景也会获得高质量编码。

真正的 segmentation-aware ROI 应该是：

```text
16×16 CU 网格

背景      +8 QP
□ □ □ □ □ □
□ □ ■ ■ □ □
□ ■ ■ ■ □ □
□ □ ■ □ □ □
□ ■ □ ■ □ □
□ □ □ □ □ □
```

然后让这些■区域获得更低 QP。

MPP 的当前 ROI utility 本身就是按 16×16 单元展开配置；RK3588 H.265 路径再以 64×64 CTU 组织这些配置。

------

# 三、我建议使用“四层 ROI”，而不是简单前景/背景两层

这是整个方案最值得做的地方。

对于 YOLO 得到的 mask：

```text
M
```

首先做 morphology：

```text
core = erode(M)

expanded = dilate(M)

edge = dilate(M) - erode(M)

halo = dilate(M) - M
```

最终得到：

```text
       halo
   ┌─────────────┐
   │    edge     │
   │   ┌─────┐   │
   │   │core │   │
   │   └─────┘   │
   │             │
   └─────────────┘

外面 = background
```

第一版我建议：

| 区域       | relative QP | 作用                   |
| ---------- | ----------- | ---------------------- |
| Background | **+8**      | 强烈压缩               |
| Halo       | +2          | 防止物体边缘和背景断裂 |
| Core       | -4          | 保持目标纹理           |
| Edge       | **-8**      | 重点保存轮廓           |

这里用的是：

```text
qp_mode = 0
```

即 **relative QP**。

MPP 当前定义明确是：

```text
qp_mode = 0 : relative QP
qp_mode = 1 : absolute QP
```

relative 模式下 `qp_val` 是对编码器最终 QP 的调整；absolute 模式则直接指定最终 QP。([GitHub](https://raw.githubusercontent.com/rockchip-linux/mpp/develop/utils/mpp_enc_roi_utils.h))

所以：

```text
qp_val = +8
```

就是：

```text
QPencoder + 8
```

而：

```text
qp_val = -8
```

就是：

```text
QPencoder - 8
```

------

# 四、为什么这里强烈建议 relative QP

不要一开始使用：

```cpp
qp_mode = 1;
qp_val = 20;
```

虽然 MPP 官方测试程序里演示了 absolute QP，但你的情况是严格 **60 kbps**。

如果一个较大目标突然进入画面：

```text
absolute QP = 18
```

可能导致：

```text
ROI bits ↑↑↑
整帧 bits ↑↑↑
瞬时码率爆炸
```

而 relative QP：

```text
encoder RC = 45kbps

ROI：
QP = base QP - 6

background：
QP = base QP + 8
```

就更容易和 CBR 协同工作。

MPP 当前文档的 CBR 模式就是以 `rc:bps_target` 为主要控制参数，同时提供 QP 范围、GOP、帧率等 RC 参数。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/doc/Rockchip_Developer_Guide_MPP_CN.md))

------

# 五、Mask → 16×16 Importance Map

假设编码输出：

```text
320 × 180
```

那么建立：

```text
ceil(320/16) × ceil(180/16)

= 20 × 12
```

的 importance map。

也就是说，真正需要处理的只有大约：

```text
240 个 cell
```

非常轻量。

建议数据结构：

```cpp
enum RoiLevel : uint8_t {
    ROI_BACKGROUND = 0,
    ROI_HALO       = 1,
    ROI_CORE       = 2,
    ROI_EDGE       = 3
};

struct RoiCell {
    RoiLevel level;
    int8_t delta_qp;
};
```

然后：

```cpp
RoiCell roi_map[12][20];
```

------

# 六、Seg mask 映射时最容易犯的错误

你的 YOLOv8-seg 很可能输入：

```text
640 × 640
```

而摄像头：

```text
1920 × 1080
```

编码：

```text
320 × 180
```

千万不要直接：

```text
640×640 mask
      ↓ resize
320×180
```

如果 YOLO 输入使用的是 letterbox，就必须：

```text
YOLO mask
   ↓
去除 letterbox padding
   ↓
映射回 camera 坐标
   ↓
再映射到 encoder 坐标
```

否则 ROI 会和真正目标偏移。

整个工程所有数据都建议带：

```cpp
struct FrameMeta {
    uint64_t frame_id;
    int64_t pts_us;

    int camera_width;
    int camera_height;

    float scale;
    int pad_x;
    int pad_y;
};
```

**frame_id 必须贯穿 Camera → YOLO → ROI → Encoder。**

这样不会出现：

```text
编码 Frame 103

却应用了
Frame 101 的 segmentation mask
```

------

# 七、16×16 cell 如何判断是不是目标

不要简单判断：

```cpp
mask[x][y] != 0
```

应该计算每个 16×16 cell 的 mask occupancy：

```text
foreground pixels
-----------------
      256
```

例如：

```text
occupancy > 0.70 → CORE
0.20 ~ 0.70       → EDGE
< 0.20            → background/halo
```

第一版可以：

```cpp
if (edge_ratio > 0.10)
    EDGE;
else if (fg_ratio > 0.60)
    CORE;
else if (dilate_ratio > 0.10)
    HALO;
else
    BACKGROUND;
```

这样 segmentation 边界会比较稳定。

------

# 八、一定要做 temporal hysteresis

YOLO segmentation 每帧都会有微小抖动。

如果直接：

```text
frame 1:

□□■■□□

frame 2:

□□□■■□

frame 3:

□□■■□□
```

编码器就会出现：

```text
CU QP
+8 → -8 → +8 → -8
```

容易产生明显质量呼吸和闪烁。

建议每个 CU 保存：

```cpp
struct RoiTemporalCell {
    RoiLevel current;
    RoiLevel previous;

    int hold_frames;
};
```

规则例如：

```text
检测到目标：
立即提升 ROI

目标消失：
不要立即恢复 background

保持 2~3 个编码帧
再逐步降低
```

例如：

```text
EDGE
 ↓
CORE
 ↓
HALO
 ↓
BACKGROUND
```

而不是：

```text
EDGE
 ↓
BACKGROUND
```

在你的 2～3 fps 场景下尤其有效。

------

# 九、第一版不要自己写底层 QP map，直接用 MPP ROI utility

这是我建议你的 **V1 实现路线**。

MPP 当前官方 encoder test 的方式就是：

```cpp
RoiRegionCfg region;

region.x = ...;
region.y = ...;
region.w = ...;
region.h = ...;

region.force_intra = 0;
region.qp_mode = 0;
region.qp_val = ...;

mpp_enc_roi_add_region(roi_ctx, &region);

...

MppMeta meta = mpp_frame_get_meta(frame);

mpp_enc_roi_setup_meta(roi_ctx, meta);

mpi->encode_put_frame(ctx, frame);
```

当前官方测试程序也是通过 `mpp_enc_roi_add_region()` 添加 ROI，然后 `mpp_enc_roi_setup_meta()` 把 ROI 挂到这一帧的 metadata。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/test/mpi_enc_test.c?utm_source=chatgpt.com))

初始化：

```cpp
MppEncRoiCtx roi_ctx = nullptr;

mpp_enc_roi_init(
    &roi_ctx,
    width,
    height,
    MPP_VIDEO_CodingHEVC,
    64
);
```

这里最后的：

```text
64
```

是你允许的最大 ROI rectangle 数。

当前 implementation 保存调用者给定的 `max_count`，添加 region 时超过这个值才拒绝；`mpp_enc_roi_setup_meta()` 完成一帧配置之后，又会把内部 region count 清零。

所以你的代码天然可以：

```text
Frame N:
build regions
setup_meta()

Frame N+1:
重新 build regions
setup_meta()
```

------

# 十、segmentation mask 怎么变成 MPP rectangle

因为公开 API 还是：

```text
x
y
w
h
qp
```

所以第一版需要做：

> **CU mask → Rectangle decomposition**

例如：

```text
■ ■ ■ □ □
■ ■ ■ □ □
■ ■ □ □ □
□ ■ □ □ □
```

不要创建 9 个：

```text
16×16 ROI
```

而是 horizontal run：

```text
row0: xxx
row1: xxx
row2: xx
row3: x
```

再进行 vertical merge：

```text
┌──────────┐
│ ■ ■ ■    │
│ ■ ■ ■    │
└──────────┘

┌──────┐
│ ■ ■  │
└──────┘

┌───┐
│ ■ │
└───┘
```

最终可能从：

```text
9 ROI
```

变成：

```text
3 ROI
```

推荐：

```text
MAX_ROI_REGION = 64
```

如果超过：

```text
1. 首先合并同 QP 邻接 rectangle
2. 删除很小的 HALO
3. EDGE 优先保留
4. CORE 次优先
5. 最后才牺牲背景策略
```

------

# 十一、这里有一个很好用的技巧：用“整帧 ROI + 前景覆盖”

你的需求是：

> 背景强压缩，前景高质量。

最方便的实现不是只添加目标 ROI。

而是第一条 ROI：

```cpp
// entire frame

x = 0;
y = 0;
w = width;
h = height;

qp_mode = 0;
qp_val = +8;
```

把全图定义成：

```text
BACKGROUND
```

然后再添加：

```text
HALO +2
CORE -4
EDGE -8
```

例如添加顺序：

```text
1 background
2 halo
3 core
4 edge
```

MPP 当前 ROI 生成逻辑按 region 顺序写入 CU 配置，因此重叠区域会被后写入的 ROI 配置覆盖；这使得这种“由低到高优先级叠加”的方法很好实现。

最终：

```text
全屏 +8

      ↓覆盖

目标周边 +2

      ↓覆盖

目标内部 -4

      ↓覆盖

目标边缘 -8
```

这就是我建议你第一版实现的核心算法。

------

# 十二、MPP 编码参数我建议从这里开始

你的物理带宽：

```text
60 kbps
```

所以不要设置：

```text
bps_target = 60000
```

建议：

```text
H265 target = 45000 bps
```

剩下：

```text
15 kbps
```

给：

- UDP/IP/RTP
- SPS/PPS
- 瞬时波动
- packet header
- pacing margin

第一组实验参数：

```text
resolution      320 × 180

fps             3

H265
CBR

bps_target      45000

GOP             30
                ≈ 10 sec

qp_min          28
qp_max          51

qp_min_i        30
qp_max_i        51
```

MPP 当前配置项对应：

```cpp
mpp_enc_cfg_set_s32(cfg,
                    "rc:mode",
                    MPP_ENC_RC_MODE_CBR);

mpp_enc_cfg_set_s32(cfg,
                    "rc:bps_target",
                    45000);

mpp_enc_cfg_set_s32(cfg,
                    "rc:fps_out_num",
                    3);

mpp_enc_cfg_set_s32(cfg,
                    "rc:fps_out_denom",
                    1);

mpp_enc_cfg_set_s32(cfg,
                    "rc:gop",
                    30);

mpp_enc_cfg_set_s32(cfg,
                    "rc:qp_min",
                    28);

mpp_enc_cfg_set_s32(cfg,
                    "rc:qp_max",
                    51);
```

这些参数名和 CBR/GOP/QP 控制均在当前 MPP 编码配置中提供。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/doc/Rockchip_Developer_Guide_MPP_CN.md))

具体 QP 数字属于我建议的**实验起点**，最终必须用你的实际场景调。

------

# 十三、不要给 segmentation ROI 设置 force_intra

也就是：

```cpp
region.force_intra = 0;
```

第一版千万不要：

```cpp
force_intra = 1;
```

因为你的带宽只有 60 kbps。

强制 intra 会破坏帧间预测优势，很容易导致：

```text
ROI 很漂亮
但是 frame size 突然变大
```

ROI 的目的应该是：

```text
保持 P-frame prediction
+
降低 ROI QP
```

不是强制 ROI 变成 intra。

MPP ROI 接口确实提供 `force_intra`，但对于你这个极低码率场景，我建议关掉。([GitHub](https://raw.githubusercontent.com/rockchip-linux/mpp/develop/utils/mpp_enc_roi_utils.h))

------

# 十四、GOP 对你这个项目非常重要

普通视频：

```text
30fps
GOP 60
```

I 帧偶尔大一点没关系。

你这里：

```text
45 kbps
```

一个 I 帧就可能占掉大量预算。

所以我建议：

```text
3 fps

GOP = 30
≈ 10秒一个IDR
```

如果网线可靠，还可以实验：

```text
GOP = 60
≈ 20秒
```

MPP 当前也提供渐进式刷新：

```text
rc:refresh_en
rc:refresh_mode
rc:refresh_num
```

因此后续还值得比较：

```text
长 GOP + IDR

vs

长 GOP + GDR/渐进刷新
```

来减少大 I 帧造成的码率尖峰。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/doc/Rockchip_Developer_Guide_MPP_CN.md))

第一版先不要同时改太多东西，先用普通 GOP 跑通。

------

# 十五、Segmentation 不一定每个编码帧都重新跑

假设：

```text
Camera = 30 fps
YOLO = 10~20 fps
H265 = 3 fps
```

完全没必要绑成：

```text
camera frame
 ↓
YOLO完成
 ↓
H265
 ↓
下一帧
```

会增加 pipeline latency。

建议线程模型：

```text
                 Capture
                    │
            ┌───────┴───────┐
            ▼               ▼
          NPU             Encoder
          Thread           Thread
            │               ▲
            ▼               │
        SegResult            │
            │                │
            ▼                │
        RoiManager───────────┘
```

核心数据结构：

```cpp
struct SegResult {
    uint64_t frame_id;
    int64_t pts_us;

    std::vector<InstanceMask> instances;
};

struct EncodeFrame {
    uint64_t frame_id;
    int64_t pts_us;

    MppBuffer buffer;
};
```

Encoder 取：

```text
最新的：

seg.frame_id <= encode.frame_id
```

但要限制年龄，例如：

```text
mask_age <= 500~700 ms
```

如果没有最新 segmentation：

```text
reuse previous ROI map
```

而不是等待 YOLO。

这对低延迟非常重要。

------

# 十六、我建议整个发送端拆成 6 个模块

工程目录可以直接这么设计：

```text
src/

├── capture/
│   ├── camera_capture.cpp
│   └── camera_capture.h
│
├── inference/
│   ├── yolov8_seg.cpp
│   └── yolov8_seg.h
│
├── roi/
│   ├── roi_mapper.cpp
│   ├── roi_mapper.h
│   ├── roi_temporal.cpp
│   └── roi_temporal.h
│
├── encoder/
│   ├── mpp_h265_encoder.cpp
│   └── mpp_h265_encoder.h
│
├── transport/
│   ├── packetizer.cpp
│   ├── rate_pacer.cpp
│   └── udp_sender.cpp
│
└── main.cpp
```

其中接口建议：

```cpp
class RoiMapper {
public:

    RoiMap build(
        const SegResult& seg,
        int encode_width,
        int encode_height
    );
};
```

Encoder：

```cpp
class H265Encoder {
public:

    void encode(
        MppBuffer frame,
        const RoiMap& roi,
        int64_t pts
    );
};
```

这样以后你把：

```text
YOLOv8-seg
```

换成：

```text
YOLO11-seg
SAM
DeepLab
自研 segmentation
```

encoder 一行不用改。

------

# 十七、MPP 集成的核心代码结构

实际每帧基本就是：

```cpp
MppFrame frame = nullptr;

mpp_frame_init(&frame);

mpp_frame_set_width(frame, width);
mpp_frame_set_height(frame, height);

mpp_frame_set_hor_stride(frame, hor_stride);
mpp_frame_set_ver_stride(frame, ver_stride);

mpp_frame_set_fmt(frame, MPP_FMT_YUV420SP);
mpp_frame_set_buffer(frame, nv12_buffer);

MppMeta meta = mpp_frame_get_meta(frame);
```

然后：

```cpp
// 1. background
add_roi(
    0, 0,
    width, height,
    +8
);

// 2. halo
for (...) {
    add_roi(..., +2);
}

// 3. core
for (...) {
    add_roi(..., -4);
}

// 4. edge
for (...) {
    add_roi(..., -8);
}
```

内部：

```cpp
void add_roi(
    int x,
    int y,
    int w,
    int h,
    int delta_qp)
{
    RoiRegionCfg region {};

    region.x = x;
    region.y = y;
    region.w = w;
    region.h = h;

    region.force_intra = 0;

    // relative
    region.qp_mode = 0;

    region.qp_val = delta_qp;

    mpp_enc_roi_add_region(
        roi_ctx_,
        &region
    );
}
```

全部添加完成：

```cpp
mpp_enc_roi_setup_meta(
    roi_ctx_,
    meta
);
```

再：

```cpp
mpi->encode_put_frame(
    ctx,
    frame
);
```

这基本就是你要添加到现有工程里的关键链路。官方当前示例也是把 ROI metadata 准备好后再 `encode_put_frame()`。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/test/mpi_enc_test.c?utm_source=chatgpt.com))

------

# 十八、第二阶段，我反而建议你直接做“真正的 QP Map”

等 V1 跑通后，我认为你的项目值得升级。

因为现在实际上是：

```text
segmentation mask

→ CU map

→ rectangle merge

→ MPP RoiRegionCfg

→ MPP内部重新展开成CU map
```

有点绕：

```text
Mask
 ↓
Grid
 ↓
Rectangle
 ↓
Grid
```

而当前 RK3588 MPP ROI 实现本来就在内部维护 CU map，并生成 H.265 的 QP buffer；RK3588 `ROI_TYPE_2` 最终向帧 metadata 写的是 `KEY_ROI_DATA2`。

所以 V2 可以直接做：

```text
YOLO mask
    ↓
16×16 delta-QP map
    ↓
MPP H265 ROI QP buffer
```

即：

```text
Mask
 ↓
Grid
 ↓
Encoder
```

可以给你自己维护的 MPP utility 增加类似：

```cpp
MPP_RET mpp_enc_roi_set_qp_map(
    MppEncRoiCtx ctx,
    const int8_t* delta_qp,
    int grid_width,
    int grid_height);
```

以后：

```cpp
qpmap[cell] = +8;   // background
qpmap[cell] = +2;   // halo
qpmap[cell] = -4;   // core
qpmap[cell] = -8;   // edge
```

不用 rectangle decomposition。

**这会是我认为这个项目最终最漂亮的实现。**

但是不建议第一阶段做，因为 `mpp_enc_roi_utils.h` 自己就注明这些 ROI 结构可能变化、不能假定二进制兼容；直接依赖内部 QP buffer 会让你和某个 MPP 版本绑定。([GitHub](https://raw.githubusercontent.com/rockchip-linux/mpp/develop/utils/mpp_enc_roi_utils.h))

所以路线应该是：

```text
V1
官方公开 ROI utility
↓
验证收益

V2
直接 segmentation QP-map
↓
减少转换、提高 mask 精度
```

------

# 十九、网络层必须单独做一个 Pacer

这是你的系统第二重要的地方。

不要：

```cpp
sendto(packet);
sendto(packet);
sendto(packet);
sendto(packet);
```

然后指望：

```text
MPP CBR = 45kbps
```

就不会超过 60 kbps。

因为 CBR 控制的是**平均编码码率**，I 帧或者复杂 P 帧仍然会产生瞬时 burst。

建议：

```text
MPP
 target = 45 kbps

        ↓

Token Bucket
 max = 50 kbps

        ↓

UDP
```

也就是：

```text
编码器负责：
生成多少 bits

Pacer负责：
这些 bits 多快送出去
```

这样即使突然一个：

```text
8 KB IDR
```

也会：

```text
逐渐发出
```

而不是一次性扔给网卡/限速队列。

------

# 二十、PC 接收端不需要 YOLO，也不需要 mask

这一点很重要。

segmentation-aware ROI：

```text
只影响 encoder 的量化策略
```

码流仍然是：

```text
普通 H.265
```

所以 PC：

```text
UDP/RTP
 ↓
HEVC depay
 ↓
H265 decoder
 ↓
frame
```

即可。

比如：

```text
FFmpeg
GStreamer
libavcodec
DXVA
NVDEC
```

都可以。

也就是说你的“还原”不是：

```text
H265 decode
+
seg mask reconstruction
```

而只是：

```text
H265 decode
```

ROI 信息根本不需要发送给 PC。

------

# 二十一、一个 RK3588 上很值得你提前检查的问题

当前 MPP develop 源码明确把：

```text
RK3588
→ ROI_TYPE_2
→ vepu58x ROI generation
```

并且 `mpp_enc_roi_init()` 允许通过环境变量 `roi_type` 覆盖自动识别。

但 Rockchip 官方 MPP 仓库有一个仍可查到的 issue：有人在 RK3588 + MPP 1.0.10 上遇到 SoC 被误识别成 RK3528，导致 ROI 走 legacy path 而不生效。([GitHub](https://github.com/rockchip-linux/mpp/issues/913?utm_source=chatgpt.com))

所以你跑第一版时，**一定验证日志**。

正确的目标应该是类似：

```text
soc = RK3588

set to vepu58x roi generation
```

如果出现：

```text
legacy roi config
```

先不要怀疑 YOLO / mask 算法。

先查：

```text
MPP version
mpp_get_soc_type()
mpp_get_soc_name()
roi_type
```

调试时可以验证：

```bash
export roi_type=2
```

是不是立即恢复 ROI 效果；生产版还是应该固定到经过你验证的 MPP 版本，而不是长期依赖环境变量强制。

------

# 二十二、最终我建议你按这 4 个阶段开发

### V0：H.265 baseline

```text
Camera
 ↓
RGA 320×180
 ↓
MPP H265
45 kbps
3 fps
 ↓
PC
```

测：

```text
bitrate
frame size
delay
PSNR/SSIM
```

------

### V1：BBox ROI

直接用 YOLO segmentation 的 instance bbox：

```text
background +8
bbox       -6
```

目的是先证明：

> MPP ROI 真正有效。

不要一开始就调复杂 segmentation。

------

### V2：Segmentation-aware ROI

加入：

```text
Mask
 ↓
16×16 map
 ↓
background +8
halo       +2
core       -4
edge       -8
 ↓
rectangle merge
 ↓
MPP ROI
```

这应该成为你的**正式第一版产品实现**。

------

### V3：Native segmentation QP map

改造/封装：

```text
mpp_enc_roi_utils
```

直接：

```text
16×16 segmentation QP map
        ↓
VEPU580 H265 QP config
```

去掉：

```text
Grid → Rectangle → Grid
```

这一层。

对于你这个 **60 kbps 极低码率 + RK3588 + 已有 YOLOv8-seg** 的组合，**V3 是我最终最推荐的技术形态**。

------

## 最后，实验一定要这样做

不要只比较：

```text
PSNR
```

应该固定：

```text
320×180
3fps
45kbps
相同视频
```

然后做三组：

| 实验  | 编码                               |
| ----- | ---------------------------------- |
| A     | 普通 H.265                         |
| B     | YOLO bbox ROI + H.265              |
| **C** | **segmentation-aware ROI + H.265** |

分别统计：

```text
全图 PSNR / SSIM

ROI PSNR / SSIM

Background PSNR / SSIM

平均 bitrate

1秒峰值 bitrate

平均 QP

端到端 latency
```

MPP encoder 的输出 metadata 本身也提供诸如 `KEY_ENC_AVERAGE_QP` 和实时码率信息，官方测试程序就是这样打印编码帧统计的，可以直接纳入你的测试日志。([GitHub](https://github.com/rockchip-linux/mpp/blob/develop/test/mpi_enc_test.c?utm_source=chatgpt.com))

**你的第一版具体实现，我建议锁定为：**

```text
Camera NV12
      │
      ├───────────── YOLOv8-seg
      │                    │
      │                    ▼
      │              mask + frame_id
      │                    │
      │                    ▼
      │             16×16 importance map
      │                    │
      │          ┌─────────┼─────────┐
      │          │         │         │
      │         +8        -4        -8
      │          BG       CORE      EDGE
      │          │         │         │
      │          └─────────┼─────────┘
      │                    ▼
      │             rectangle merge
      │                    │
      ▼                    ▼
    MppFrame ─────→ ROI metadata
                     │
                     ▼
                 H265 MPP
                 45 kbps
                     │
                     ▼
                 50 kbps
               token bucket
                     │
                     ▼
                    PC
```

如果下一步进入代码实现，我建议直接从 **`RoiMapper` 这一层**开始：我可以按你现有 YOLOv8-seg 输出的 `mask/box/class/confidence` 数据结构，给你写出一版 **C++ 的 `mask → 16×16网格 → core/edge/halo → rectangle merge → MPP RoiRegionCfg` 完整代码**，这一块基本就是整个方案的核心代码。