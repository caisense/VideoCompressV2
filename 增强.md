**本项目建议保留发送端 YOLO**。接收端图像增强和发送端 YOLO 解决的是两个不同问题：

| 接收端增强方式                             | 是否需要发送端 YOLO                     |
| ------------------------------------------ | --------------------------------------- |
| 全画面降噪、去块、Lanczos放大、锐化、CLAHE | 不需要                                  |
| 全画面 Real-ESRGAN                         | 不强制需要，但计算量大                  |
| 只增强人物、车辆等 ROI                     | 需要发送ROI元数据，或接收端重新运行YOLO |
| Kimi/GLM语义叠加、状态插值                 | 需要发送端YOLO/跟踪器                   |
| DeepSeek式语义生成720P                     | 必须有语义分割，可放发送端或接收端      |
| 当前分割ROI/QP编码                         | 需要发送端YOLO                          |

关键原因是：**接收端增强无法恢复发送端编码时已经丢失的信息**。

当前项目中，YOLOv8-Seg在编码前生成ROI/QP图，让MPP优先保留人物、车辆、边缘等区域；PC收到的仍是标准H.265，并没有单独收到分割图层 [README.md (line 170)](/D:/workspace/atk_yolov8_seg_cam_v4/README.md:170)。如果删除发送端YOLO，编码器会平均分配极少的码率，目标区域可能先被压糊，接收端超分只能让模糊画面看起来更平滑，不能还原真实细节。



# QP

QP 是“量化参数”（Quantization Parameter），决定 H.265 编码时丢弃多少图像细节。

- QP 越小：画质越清晰，码率越高。
- QP 越大：细节丢失越多，画质越差，码率越低。
- H.265 常用范围为 `0～51`。

本项目的 ROI/QP 是让不同区域使用不同QP：

- 人物边缘：降低QP，优先保持轮廓清晰。
- 人物内部：适当降低QP。
- 周围缓冲区：正常或略微降低。
- 背景：提高QP，节省带宽。

例如基础QP为38：

| 区域     | QP偏移 | 大致QP | 效果   |
| -------- | ------ | ------ | ------ |
| 边缘     | `-10`  | 28     | 最清晰 |
| 目标内部 | `-6`   | 32     | 较清晰 |
| 缓冲区   | `+2`   | 40     | 略模糊 |
| 背景     | `+12`  | 50     | 强压缩 |

因此，ROI/QP的本质是：**把有限码率优先分配给人物、车辆及其边缘，而不是平均压缩整张画面。**它是板端编码控制参数，不是单独发送给PC的图层。